import React from "react";

const UserContext = React.createContext(); // think it is global variable, we need Provider who provides value to all components, elements which are wrapped into it.

export default UserContext;